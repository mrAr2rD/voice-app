<!DOCTYPE html>
<html lang="ru" class="noise">
  <head>
    <title>Админ-панель — AppCaster</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="#0a0a0f">
    <meta name="turbo-cache-control" content="no-cache">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      .bg-gray-50 { background: var(--color-void) !important; }
      .bg-white { background: var(--color-abyss) !important; }
      .text-gray-900 { color: var(--color-snow) !important; }
      .text-gray-700 { color: var(--color-cloud) !important; }
      .text-gray-600 { color: var(--color-cloud) !important; }
      .text-gray-500 { color: var(--color-mist) !important; }
      .border-gray-200 { border-color: var(--color-graphite) !important; }
      .border-gray-300 { border-color: var(--color-slate) !important; }
    </style>
  </head>

  <body class="min-h-screen" style="background: var(--color-void);">
    <div class="flex min-h-screen">
      <!-- Sidebar -->
      <aside class="w-64 flex flex-col" style="background: var(--color-abyss); border-right: 1px solid var(--color-graphite);">
        <!-- Logo -->
        <div class="p-6" style="border-bottom: 1px solid var(--color-graphite);">
          <div class="flex items-center">
            <div class="w-10 h-10 rounded-xl flex items-center justify-center"
                 style="background: var(--gradient-aurora);">
              <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
              </svg>
            </div>
            <div class="ml-3">
              <span class="text-lg font-bold" style="color: var(--color-snow);">Admin</span>
              <span class="text-xs block" style="color: var(--color-mist);">AppCaster</span>
            </div>
          </div>
        </div>

        <!-- Navigation -->
        <nav class="flex-1 p-4 space-y-2">
          <%= link_to admin_root_path, data: { turbo: false },
              class: "flex items-center px-4 py-3 rounded-xl transition-all duration-200 group",
              style: request.path == admin_root_path ?
                "background: rgba(0, 212, 255, 0.1); color: var(--color-neon-blue); border: 1px solid rgba(0, 212, 255, 0.2);" :
                "color: var(--color-cloud); border: 1px solid transparent;" do %>
            <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
            </svg>
            Дашборд
          <% end %>

          <%= link_to admin_settings_path, data: { turbo: false },
              class: "flex items-center px-4 py-3 rounded-xl transition-all duration-200 group",
              style: request.path == admin_settings_path ?
                "background: rgba(0, 212, 255, 0.1); color: var(--color-neon-blue); border: 1px solid rgba(0, 212, 255, 0.2);" :
                "color: var(--color-cloud); border: 1px solid transparent;" do %>
            <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"/>
            </svg>
            Настройки
          <% end %>
        </nav>

        <!-- Footer -->
        <div class="p-4" style="border-top: 1px solid var(--color-graphite);">
          <%= link_to root_path, data: { turbo: false },
              class: "flex items-center px-4 py-3 rounded-xl transition-all duration-200",
              style: "color: var(--color-mist);" do %>
            <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 17l-5-5m0 0l5-5m-5 5h12"/>
            </svg>
            На сайт
          <% end %>

          <div class="mt-3 px-4 py-2 text-sm" style="color: var(--color-mist);">
            <%= current_user.email %>
          </div>
        </div>
      </aside>

      <!-- Main content -->
      <main class="flex-1 overflow-auto">
        <!-- Top bar -->
        <header class="px-8 py-4" style="background: var(--color-abyss); border-bottom: 1px solid var(--color-graphite);">
          <div class="flex items-center justify-between">
            <h2 class="text-xl font-bold" style="color: var(--color-snow);">
              <% if request.path == admin_root_path %>
                Панель управления
              <% elsif request.path == admin_settings_path %>
                Настройки системы
              <% end %>
            </h2>
            <div class="text-sm" style="color: var(--color-mist);">
              <%= Time.current.strftime("%d.%m.%Y %H:%M") %>
            </div>
          </div>
        </header>

        <!-- Content -->
        <div class="p-8">
          <% if flash[:notice] %>
            <div class="mb-6 animate-slide-down">
              <div class="glass rounded-xl p-4 border-l-4" style="border-left-color: var(--color-neon-green);">
                <div class="flex items-center">
                  <svg class="w-5 h-5 mr-3" style="color: var(--color-neon-green);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                  </svg>
                  <span style="color: var(--color-snow);"><%= flash[:notice] %></span>
                </div>
              </div>
            </div>
          <% end %>

          <% if flash[:alert] %>
            <div class="mb-6 animate-slide-down">
              <div class="glass rounded-xl p-4 border-l-4" style="border-left-color: var(--color-coral);">
                <div class="flex items-center">
                  <svg class="w-5 h-5 mr-3" style="color: var(--color-coral);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                  </svg>
                  <span style="color: var(--color-snow);"><%= flash[:alert] %></span>
                </div>
              </div>
            </div>
          <% end %>

          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>

<!DOCTYPE html>
<html lang="ru" class="noise">
  <head>
    <title><%= content_for(:title) || "VoiceApp — Личный кабинет" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="VoiceApp">
    <meta name="theme-color" content="#0a0a0f">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen" style="background: var(--color-void);">
    <div class="flex min-h-screen">
      <!-- Sidebar -->
      <aside class="w-64 fixed left-0 top-0 h-full z-20 flex flex-col"
             style="background: var(--color-abyss); border-right: 1px solid var(--color-graphite);">
        <!-- Logo -->
        <div class="p-6 border-b" style="border-color: var(--color-graphite);">
          <%= link_to root_path, class: "flex items-center" do %>
            <span class="text-2xl font-bold text-gradient">VoiceApp</span>
          <% end %>
        </div>

        <!-- Navigation -->
        <nav class="flex-1 p-4 space-y-2 overflow-y-auto">
          <p class="px-3 py-2 text-xs font-semibold uppercase tracking-wider" style="color: var(--color-mist);">
            Сервисы
          </p>

          <%= link_to transcriptions_path,
              class: "flex items-center px-4 py-3 rounded-xl transition-all duration-200 group #{controller_name == 'transcriptions' ? 'bg-white/10' : 'hover:bg-white/5'}",
              style: controller_name == 'transcriptions' ? "background: rgba(0, 212, 255, 0.1); border: 1px solid rgba(0, 212, 255, 0.2);" : "" do %>
            <div class="w-10 h-10 rounded-lg flex items-center justify-center mr-3"
                 style="background: rgba(0, 212, 255, 0.1);">
              <svg class="w-5 h-5" style="color: var(--color-neon-blue);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11a7 7 0 01-7 7m0 0a7 7 0 01-7-7m7 7v4m0 0H8m4 0h4m-4-8a3 3 0 01-3-3V5a3 3 0 116 0v6a3 3 0 01-3 3z"/>
              </svg>
            </div>
            <div>
              <p class="font-medium" style="color: var(--color-snow);">Транскрибация</p>
              <p class="text-xs" style="color: var(--color-mist);">Аудио в текст</p>
            </div>
          <% end %>

          <%= link_to voice_generations_path,
              class: "flex items-center px-4 py-3 rounded-xl transition-all duration-200 group #{controller_name == 'voice_generations' ? 'bg-white/10' : 'hover:bg-white/5'}",
              style: controller_name == 'voice_generations' ? "background: rgba(168, 85, 247, 0.1); border: 1px solid rgba(168, 85, 247, 0.2);" : "" do %>
            <div class="w-10 h-10 rounded-lg flex items-center justify-center mr-3"
                 style="background: rgba(168, 85, 247, 0.1);">
              <svg class="w-5 h-5" style="color: var(--color-neon-purple);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.536 8.464a5 5 0 010 7.072m2.828-9.9a9 9 0 010 12.728M5.586 15H4a1 1 0 01-1-1v-4a1 1 0 011-1h1.586l4.707-4.707C10.923 3.663 12 4.109 12 5v14c0 .891-1.077 1.337-1.707.707L5.586 15z"/>
              </svg>
            </div>
            <div>
              <p class="font-medium" style="color: var(--color-snow);">Озвучка</p>
              <p class="text-xs" style="color: var(--color-mist);">Текст в речь</p>
            </div>
          <% end %>

          <%= link_to translations_path,
              class: "flex items-center px-4 py-3 rounded-xl transition-all duration-200 group #{controller_name == 'translations' ? 'bg-white/10' : 'hover:bg-white/5'}",
              style: controller_name == 'translations' ? "background: rgba(34, 211, 187, 0.1); border: 1px solid rgba(34, 211, 187, 0.2);" : "" do %>
            <div class="w-10 h-10 rounded-lg flex items-center justify-center mr-3"
                 style="background: rgba(34, 211, 187, 0.1);">
              <svg class="w-5 h-5" style="color: var(--color-neon-green);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5h12M9 3v2m1.048 9.5A18.022 18.022 0 016.412 9m6.088 9h7M11 21l5-10 5 10M12.751 5C11.783 10.77 8.07 15.61 3 18.129"/>
              </svg>
            </div>
            <div>
              <p class="font-medium" style="color: var(--color-snow);">Переводчик</p>
              <p class="text-xs" style="color: var(--color-mist);">AI перевод</p>
            </div>
          <% end %>

          <div class="pt-4 mt-4" style="border-top: 1px solid var(--color-graphite);">
            <p class="px-3 py-2 text-xs font-semibold uppercase tracking-wider" style="color: var(--color-mist);">
              Аккаунт
            </p>

            <%= link_to profile_path,
                class: "flex items-center px-4 py-3 rounded-xl transition-all duration-200 hover:bg-white/5" do %>
              <div class="w-10 h-10 rounded-lg flex items-center justify-center mr-3"
                   style="background: var(--color-graphite);">
                <svg class="w-5 h-5" style="color: var(--color-cloud);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                </svg>
              </div>
              <div>
                <p class="font-medium" style="color: var(--color-snow);">Профиль</p>
                <p class="text-xs" style="color: var(--color-mist);">Настройки</p>
              </div>
            <% end %>

            <% if current_user&.admin? %>
              <%= link_to admin_root_path,
                  class: "flex items-center px-4 py-3 rounded-xl transition-all duration-200 hover:bg-white/5" do %>
                <div class="w-10 h-10 rounded-lg flex items-center justify-center mr-3"
                     style="background: rgba(251, 191, 36, 0.1);">
                  <svg class="w-5 h-5" style="color: var(--color-gold);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                  </svg>
                </div>
                <div>
                  <p class="font-medium" style="color: var(--color-snow);">Админка</p>
                  <p class="text-xs" style="color: var(--color-mist);">Управление</p>
                </div>
              <% end %>
            <% end %>
          </div>
        </nav>

        <!-- User section -->
        <div class="p-4 border-t" style="border-color: var(--color-graphite);">
          <div class="flex items-center justify-between">
            <div class="flex items-center min-w-0">
              <div class="w-10 h-10 rounded-full flex items-center justify-center flex-shrink-0"
                   style="background: var(--gradient-aurora);">
                <span class="text-sm font-bold" style="color: var(--color-void);">
                  <%= current_user&.name&.first&.upcase %>
                </span>
              </div>
              <div class="ml-3 min-w-0">
                <p class="font-medium truncate" style="color: var(--color-snow);"><%= current_user&.name %></p>
                <p class="text-xs truncate" style="color: var(--color-mist);"><%= current_user&.email %></p>
              </div>
            </div>
            <%= button_to logout_path, method: :delete,
                class: "p-2 rounded-lg transition-colors hover:bg-white/10",
                title: "Выйти" do %>
              <svg class="w-5 h-5" style="color: var(--color-mist);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
              </svg>
            <% end %>
          </div>
        </div>
      </aside>

      <!-- Main content -->
      <main class="flex-1 ml-64">
        <!-- Top bar -->
        <header class="sticky top-0 z-10 px-8 py-4 border-b"
                style="background: var(--color-void); border-color: var(--color-graphite);">
          <div class="flex items-center justify-between">
            <h1 class="text-xl font-bold" style="color: var(--color-snow);">
              <%= content_for(:page_title) || "Личный кабинет" %>
            </h1>
            <% if content_for?(:page_actions) %>
              <%= yield :page_actions %>
            <% end %>
          </div>
        </header>

        <!-- Content -->
        <div class="p-8">
          <% if flash[:notice] %>
            <div class="mb-6 animate-slide-down">
              <div class="glass rounded-xl p-4 border-l-4" style="border-left-color: var(--color-neon-green);">
                <div class="flex items-center">
                  <svg class="w-5 h-5 mr-3" style="color: var(--color-neon-green);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                  </svg>
                  <span style="color: var(--color-snow);"><%= flash[:notice] %></span>
                </div>
              </div>
            </div>
          <% end %>

          <% if flash[:alert] %>
            <div class="mb-6 animate-slide-down">
              <div class="glass rounded-xl p-4 border-l-4" style="border-left-color: var(--color-coral);">
                <div class="flex items-center">
                  <svg class="w-5 h-5 mr-3" style="color: var(--color-coral);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                  </svg>
                  <span style="color: var(--color-snow);"><%= flash[:alert] %></span>
                </div>
              </div>
            </div>
          <% end %>

          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>
